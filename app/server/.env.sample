# API Keys for LLM providers
# You need at least one of these to use the natural language to SQL feature
# Gemini is the primary LLM provider for this project
GEMINI_API_KEY=your-gemini-api-key-here
# Anthropic - Uses Claude Code subscription (set to "claude-code-subscription" or your own API key)
ANTHROPIC_API_KEY=claude-code-subscription